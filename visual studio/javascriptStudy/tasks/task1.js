export function tasks(name, age, isStudent) {
    return {
        name: "surafel",
        age: 19,
        isStudent: true
    }

    console.log(name, age, isStudent);
}

export default tasks();