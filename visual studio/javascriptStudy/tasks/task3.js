import tasks from './task7.js';

let person = {
    name: name,
    age: age,
    address: {
        street: "addis ababa",
    },
};

console.log(person.age);
console.log(person.name);
console.log(person.address.street);

import tasks from './task7.js';